---
name: Run Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  pytest:
    runs-on: ${{ matrix.runs_on }}
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
        runs_on:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
      - name: Checkout code
        # v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Set up Python ${{ matrix.python-version }}
        # v6.2.0
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies via apt
        if: matrix.runs_on == 'ubuntu-latest'
        run: |
          sudo apt install libcurl4-openssl-dev

      - name: Install dependencies via uv
        # v8
        uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run pytest
        run: |
          uv run --frozen pytest

  integration:
    runs-on: ${{ matrix.runs_on }}
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
        runs_on:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
      - name: Checkout code
        # v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Set up Python ${{ matrix.python-version }}
        # v6.2.0
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies via apt
        if: matrix.runs_on == 'ubuntu-latest'
        run: |
          sudo apt install libcurl4-openssl-dev

      - name: Install dependencies via uv
        # v8
        uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57
        with:
          python-version: ${{ matrix.python-version }}

      - name: Generate example analysis (Linux)
        if: matrix.runs_on == 'ubuntu-latest'
        run: |
          cd examples
          rm *.csv
          uv run --frozen sh generate_examples.sh

      - name: Generate example analysis (macOS)
        if: matrix.runs_on == 'macos-latest'
        run: |
          cd examples
          rm *.csv
          uv run --frozen sh generate_examples.sh

      - name: Generate example analysis (Windows)
        if: matrix.runs_on == 'windows-latest'
        run: |
          cd examples
          del *.csv
          uv run --frozen cmd.exe generate_examples.bat

      - name: Verify the outputs
        run:
          git diff --exit-code examples
